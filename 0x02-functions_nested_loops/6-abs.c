#include "main.h"

/**
 * _abs - functions that get absolute value of number
 *
 * @n: @ab: integer input
 *
 * Return: abolute value of the number
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
